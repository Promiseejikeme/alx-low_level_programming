#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * str: takes in a string
 * _put: returns a string
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
