#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * str: stores some string
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lac of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
