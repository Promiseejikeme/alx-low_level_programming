#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - checks the code
 * @argc: counts the number arguments
 * @argv: array of pointersto the string
 * Return: returns EXIT_SUCCESS
 */
int main(int argc, char *argv[])
{
	_putchar('0' + argc);
	_putchar('\n');
	return (EXIT_SUCCESS);
}
